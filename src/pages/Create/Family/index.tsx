import { FC } from 'react';

import HelmetTitle from '~/ui-kit/atoms/Meta/';

const Family: FC = () => (
  <>
    <HelmetTitle title='Create Family' />
    <h1>Family</h1>
  </>
);
export default Family;
